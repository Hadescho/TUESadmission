<!DOCTYPE html>
<html>
<head>
  <title>Admission</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
</head>
<body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="user_auth" style="float:right">
    	<%if user_signed_in? %>
    		Логнат сте като: <%= current_user.email %>
    		<%= link_to "Излезте от профила си", destroy_user_session_path, method: :delete%>
    	<% else %>
    		<%= link_to "Влезте в профила си", new_user_session_path %>
    	<% end %>
    </div>

    <div class="campaign_select" style="float:center">
    <% campaigns_names = []; campaigns_ids = [] %>
    <% Campaign.all.each {|c| campaigns_names<< [c.name,c.id]} %>
            <%= select_tag "campaigns", options_for_select(campaigns_names,campaigns_ids), onchange:"document.location = '/campaigns/' + this.value" %>
    </div>
<%= yield %>


<p>WARNING : config.active_record.whitelist_attributes is set to false ! </p>

</body>
</html>
