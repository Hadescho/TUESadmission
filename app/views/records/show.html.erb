<table>
	<tr>
		<td>
		<!-- Image of the student -->
		</td>
		<td>
			<table>
				<% @record.properties.each do |key,value| %>
				<tr>
					<td>
						<%= key %>:
					</td>
					<td>
						<%= value %>
					</td>
				</tr>
				<% end %>
			</table><br>

		<%= link_to "Генерирай документ", 
			request.url + '.pdf' 
		%> |
				
		<%= link_to "Промени", 
			root_url + "campaigns/#{@record.campaign_id}/records/#{@record.id}", 
			method: "update"
		%> |

		<%= link_to "Изтрий", 
			root_url + "campaigns/#{@record.campaign_id}/records/#{@record.id}", 
			method: "delete" 
		%>
	
		</td>
	</tr>	

</table>
