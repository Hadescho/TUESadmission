<%= form_for @record_type do |f|%>
	Име:<%= f.text_field :name %><br>

	<%= f.fields_for :fields do |builder| %>
		<%= render 'field_fields', f: builder%>
	<% end %>
	<%= link_to_add_fields "Добави поле", f, :fields %>

	<%= f.submit %>
<% end %>